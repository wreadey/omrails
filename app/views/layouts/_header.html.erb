<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
             <!-- <a class="brand" href="/">One Month Rails</a> -->
             <%= link_to "paint the street NYC", root_path, class: "brand" %>

                <div class="nav-collapse">
                    <ul class="nav">
                        <li><%= link_to content_tag(:i, "", class:"icon-home icon-2x"),  root_path %></li>
                        <li><%= link_to content_tag(:i, "", class:"icon-info-sign icon-2x"),  about_path %></li>
                    </ul>
                    <ul class="nav pull-right">
                        <% if user_signed_in? %>
                            <li><%= link_to content_tag(:i, "", class:"icon-plus icon-2x")+" Add", new_pin_path %></li>
                            <li>
                                <%  # This doesn't work so well with the nav-collapse responsive feature.
                                    # Look into fixes later.

                                    # Holy hack! I think the link hack to the user's profile from the part with the user name on it will work!
                                %>
                                <div class="btn-group">  
                                  <a class="btn btn-primary" href="/users/<%= current_user.id %>"><i class="icon-user icon-white"></i> <%= current_user.name %></a>  
                                  <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>  
                                  <ul class="dropdown-menu">  
                                    <li><%= link_to content_tag(:i, "", class:"icon-pencil icon-2x")+" Edit Profile",  edit_user_registration_path %></li>  
                                    <li><%= link_to content_tag(:i, "", class:"icon-trash icon-2x")+" Logout",  destroy_user_session_path, method: :delete %></li>  
                                  </ul>  
                                </div>  
                            </li>
                        <% else %>
                            <li><%= link_to 'Login',  new_user_session_path %></li>
                        <% end %>
                    </ul>
                </div>
        </div>
    </div>
</div>